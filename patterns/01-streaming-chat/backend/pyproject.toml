[project]
name = "genkit-fastapi-starter"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "genkit",
    "genkit-plugin-google-genai",
    "genkit-plugin-fastapi",
    "fastapi",
    "uvicorn",
]
